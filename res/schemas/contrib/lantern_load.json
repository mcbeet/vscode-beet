{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "id": {
      "title": "Lantern Load project id",
      "description": "Defaults to the project name",
      "type": "string"
    },
    "version": {
      "title": "Lantern Load version",
      "description": "Defaults to the project version",
      "type": "string"
    },
    "dependencies": {
      "title": "Project dependencies",
      "type": "object",
      "additionalProperties": {
        "anyOf": [{ "type": "string" }, { "type": "number" }]
      }
    }
  }
}
