{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "match": {
      "description": "Add the header to the selected functions",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "template": {
      "description": "The name of the template. Defaults to \"function_header.mcfunction\"",
      "type": "string",
      "pattern": ".*\\.mcfunction"
    }
  }
}
